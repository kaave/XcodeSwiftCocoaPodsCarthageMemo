# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SwiftProjectMemo' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SwiftProjectMemo
  pod 'SwiftLint'

  target 'SwiftProjectMemoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SwiftProjectMemoUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
